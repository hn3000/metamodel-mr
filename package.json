{
  "name": "metamodel-mr",
  "description": "metamodel (+friends) monorepo",
  "workspaces": [
    "./modules/*"
  ],
  "scripts": {
    "test": "npm --workspaces test",
    "tsc": "npm --workspaces run tsc"
  }
}